Testando branch

stash{
    git stash
    git stash list
    git stash apply {id}
    git stash show -p {id}
    git stash drop {id}
    git stash clear
}

tag{
    git tag
    git tag -a v1.0 -m "comentário"
    git show {tagName}
    git checkout {tagName}
}